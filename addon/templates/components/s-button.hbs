{{#if actionName}}
  <button
    class="btn btn-{{color}} {{className}}"
    disabled={{disabled}}
    {{action actionName}}
  >
    {{yield}}
  </button>
{{/if}}

{{#if link}}
  {{#link-to link class=(concat "btn fs12 " className "btn-"color)}}
    {{yield}}
  {{/link-to}}
{{/if}}