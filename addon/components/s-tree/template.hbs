{{ember-jstree
    actionReceiver=actionReceiver
    data=data
    typesOptions=typesOptions
    themes=themes
    checkboxOptions=checkboxOptions
    plugins=plugins
    eventDidSelectNode=(action eventDidSelectNode)

    searchOptions=searchOptions
    searchTerm=searchTerm
}}
{{!-- eventDidDeselectNode=(action eventDidDeselectNode) --}}