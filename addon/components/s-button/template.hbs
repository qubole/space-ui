{{#if actionName}}
    <button class="btn {{className}}" {{action actionName }} disabled={{disabled}}>
        {{yield}}
    </button>
{{/if}}

{{#if link}}
    {{#link-to link class=(concat "btn fs12 " className)}}
        {{yield}}
    {{/link-to}}
{{/if}}